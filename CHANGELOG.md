## Styles Improvements
- Add `Styles.use_plain()` - switches parse mode to None
- Add `user_link`, `bot_link`, `link` to `Styles` class
- Fix `BotLink` – bot ID cannot be used in links; only the bot username is supported